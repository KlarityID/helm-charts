{{- if .Values.services.webService.external }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.environment | lower }}-{{ .Values.webservice.name | lower }}
  namespace: {{ .Values.namespace | default "default" }}
  labels:
    app: {{ .Values.environment | lower }}-{{ .Values.webservice.name | lower }}
    environment: {{ .Values.environment | lower }}
    webservice: {{ .Values.webservice.name | lower }}
    app.kubernetes.io/name: {{ .Release.Name }}
spec:
  type: {{ .Values.services.webService.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.services.webService.port }}
      targetPort: {{ .Values.services.webService.port }}
      protocol: TCP
  selector:
    app: {{ .Values.environment | lower }}-{{ .Values.webservice.name | lower }}
    environment: {{ .Values.environment | lower }}
    webservice: {{ .Values.webservice.name | lower }}
    app.kubernetes.io/name: {{ .Release.Name }}


# Ingress Route
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Values.environment | lower }}-{{ .Values.webservice.name | lower }}
  namespace: {{ .Values.namespace | default "default" }}
  labels:
    app: {{ .Values.environment | lower }}-{{ .Values.webservice.name | lower }}
    environment: {{ .Values.environment | lower }}
    webservice: {{ .Values.webservice.name | lower }}
    app.kubernetes.io/name: {{ .Release.Name }}
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`{{ .Values.services.webService.hostSuffix }}.{{ .Values.domain }}`)
      services:
        - kind: Service
          name: {{ .Values.environment | lower }}-{{ .Values.webservice.name | lower }}
          port: {{ .Values.services.webService.port }}
{{- end }}